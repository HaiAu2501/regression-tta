name: svhn_mnist
module: data.svhn_mnist

source:
  name: svhn
target:
  name: mnist

dataloader:
  train:
    batch_size: 64
    shuffle: true
  adapt:
    batch_size: 64
    shuffle: true
  eval:
    batch_size: 256
    shuffle: false
