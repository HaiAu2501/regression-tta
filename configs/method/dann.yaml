name: dann
module: baselines.dann

optimizer:
  params: fe_bn
  name: Adam
  lr: 0.001
  weight_decay: 0.0

discriminator:
  hidden_dim: 1024
  lr: 0.001
  weight_decay: 0.0

lambda_init: 0.0
lambda_final: 1.0
